# Podfile
platform :ios, '16.0'
use_frameworks!

# Note: project directive is omitted to allow CocoaPods to auto-detect the project
# The project file (MyPal.xcodeproj) is generated by 'xcodegen generate' during 'make generate'
# If the project doesn't exist, CocoaPods will provide a clearer error message
# Always run 'make generate' before 'pod install' to ensure the project exists

target 'MyPal' do
  pod 'MediaPipeTasksGenAI'
end

