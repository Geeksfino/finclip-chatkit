# ============================================
# Server Configuration
# ============================================
PORT=3000
HOST=0.0.0.0

# ============================================
# Agent Mode
# ============================================
# Agent runtime mode: emulated | llm
# - emulated: Use scenario/echo mode (no real LLM, for testing). DEFAULT_SCENARIO and
#   scenarios in SCENARIO_DIR are used. Set DEFAULT_SCENARIO=echo for echo mode.
# - llm: Use real LLM provider (requires valid LLM_API_KEY and LLM config below).
# Can be overridden by CLI: --use-llm or --emulated
AGENT_MODE=emulated

# ============================================
# LLM Provider Configuration
# ============================================
# Provider type: deepseek | openai | siliconflow | litellm
# LLM_API_KEY is required when AGENT_MODE=llm
LLM_PROVIDER=deepseek

# Model name
LLM_MODEL=deepseek-chat

# API Key (required when AGENT_MODE=llm)
LLM_API_KEY=your-api-key-here

# Base URL (optional, uses provider default if empty)
# deepseek: https://api.deepseek.com/v1
# openai: https://api.openai.com/v1
# siliconflow: https://api.siliconflow.cn/v1
# litellm: http://localhost:4000/v1
LLM_BASE_URL=

# Provider timeout settings
LLM_TIMEOUT_MS=60000
LLM_MAX_RETRIES=2
LLM_RETRY_DELAY_MS=1000

# ============================================
# Extension Mode (MCPUI / A2UI)
# ============================================
# none (default) | mcpui | a2ui
# - mcpui: Enable MCPUI tools from mcpui-test-server, LLM can call showSimpleHtml etc.
# - a2ui: Proxy to a2ui-test-server, emit CUSTOM (a2ui) events
EXTENSION_MODE=none
# MCPUI server URL (used when EXTENSION_MODE=mcpui)
# MCP endpoint, e.g. http://localhost:3100/mcp
MCPUI_SERVER_URL=http://localhost:3100/mcp
# A2UI server URL (used when EXTENSION_MODE=a2ui)
# Base URL, e.g. http://localhost:3200
A2UI_SERVER_URL=http://localhost:3200
# A2UI proxy timeout (ms)
A2UI_TIMEOUT_MS=60000
# MCP connect timeout (ms)
MCP_CONNECT_TIMEOUT_MS=5000
# MCP tool call timeout (ms)
MCP_TOOL_CALL_TIMEOUT_MS=30000

# ============================================
# Agent / Scenario Configuration
# ============================================
# Default scenario ID (used when AGENT_MODE=emulated)
# - echo: Echo mode, repeats user input
# - tool-call, simple-chat, error-handling: JSON scenario files in SCENARIO_DIR
DEFAULT_SCENARIO=tool-call
# Scenario JSON files directory
SCENARIO_DIR=./src/scenarios
# Delay between scenario steps (ms)
SCENARIO_DELAY_MS=200

# ============================================
# SSE Configuration
# ============================================
# Client reconnection interval (ms)
SSE_RETRY_MS=3000
# Server heartbeat interval (ms)
SSE_HEARTBEAT_MS=30000

# ============================================
# Logging
# ============================================
# Log level: debug | info | warn | error
LOG_LEVEL=info
# Enable colored/formatted log output
LOG_PRETTY=true

# ============================================
# CORS
# ============================================
CORS_ORIGIN=*
